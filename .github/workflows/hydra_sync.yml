name: Hydra Sync Protocol 🐍 (Native)

on:
  push:
    branches: [ main ]
    # Runs automatically whenever you update the Main account

jobs:
  distribute_traffic:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0

      # =========================================================
      # 1️⃣ MIRROR 1 ‎otakuhero99-lang
      # =========================================================
      - name: ‎otakuhero99-lang
        run: |
          # ⬇️ REPLACE 'DevCache99' WITH YOUR ACTUAL BACKUP USERNAME ⬇️
          git remote add mirror1 https://otakuhero99-lang:${{ secrets.PAT_MIRROR_1 }}@github.com/otakuhero99-lang/anime-index.git

          git push mirror1 main --force
          git remote remove mirror1

      # =========================================================
      # 2️⃣ MIRROR 2 balwindar-wq
      # =========================================================
      - name: balwindar-wq
        run: |
          # ⬇️ REPLACE 'ProjectRonin' WITH YOUR ACTUAL BACKUP USERNAME ⬇️
          git remote add mirror2 https://balwindar-wq:${{ secrets.PAT_MIRROR_2 }}@github.com/balwindar-wq/anime-index.git
          git push mirror2 main --force
          git remote remove mirror2

      # =========================================================
      # 3️⃣ MIRROR 3 otakverse01
      # =========================================================
      - name: otakverse01
        run: |
          # ⬇️ REPLACE 'NeonStack' WITH YOUR ACTUAL BACKUP USERNAME ⬇️
          git remote add mirror3 https://otakverse01:${{ secrets.PAT_MIRROR_3 }}@github.com/otakverse01/anime-index.git
          git push mirror3 main --force
          git remote remove mirror3

      # =========================================================
      # 1️⃣ MIRROR 1 verseotak01-dot
      # =========================================================
      - name: verseotak01-dot
        run: |
          # ⬇️ REPLACE 'DevCache99' WITH YOUR ACTUAL BACKUP USERNAME ⬇️
          git remote add mirror1 https://verseotak01-dot:${{ secrets.PAT_MIRROR_4 }}@github.com/verseotak01-dot/anime-index.git
          git push mirror1 main --force
          git remote remove mirror1

      # =========================================================
      # 2️⃣ MIRROR 2 OtaKuNexa
      # =========================================================
      - name: OtaKuNexa
        run: |
          # ⬇️ REPLACE 'ProjectRonin' WITH YOUR ACTUAL BACKUP USERNAME ⬇️
          git remote add mirror2 https://OtaKuNexa:${{ secrets.PAT_MIRROR_5 }}@github.com/OtaKuNexa/anime-index.git
          git push mirror2 main --force
          git remote remove mirror2

      # =========================================================
      # 3️⃣ MIRROR 3 hokaiwatch 
      # =========================================================
      - name: hokaiwatch
        run: |
          # ⬇️ REPLACE 'NeonStack' WITH YOUR ACTUAL BACKUP USERNAME ⬇️
          git remote add mirror3 https://hokaiwatch:${{ secrets.PAT_MIRROR_6 }}@github.com/hokaiwatch/anime-index.git
          git push mirror3 main --force
          git remote remove mirror3
          

